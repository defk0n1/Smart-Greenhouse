[core]
# The executor class that airflow should use
executor = CeleryExecutor

# The folder where your airflow pipelines live
dags_folder = /opt/airflow/dags

# The folder where airflow should store its log files
base_log_folder = /opt/airflow/logs

# Hostname by providing a path to a callable
hostname_callable = socket.getfqdn

# Default timezone in case supplied date times are naive
default_timezone = utc

[database]
# The SqlAlchemy connection string to the metadata database
sql_alchemy_conn = postgresql+psycopg2://airflow:airflow@postgres/airflow

[celery]
# The Celery broker URL
broker_url = redis://redis:6379/0

# The Celery result_backend
result_backend = db+postgresql://airflow:airflow@postgres/airflow

[webserver]
# The base url of your website
base_url = http://localhost:8080

# The ip specified when starting the web server
web_server_host = 0.0.0.0

# The port on which to run the web server
web_server_port = 8080

# Number of seconds the webserver waits before killing gunicorn master
web_server_master_timeout = 120

# Number of seconds the webserver waits before timing out a worker
web_server_worker_timeout = 120

# Secret key used to run your flask app
secret_key = your-secret-key-here

# Number of workers to run the Gunicorn web server
workers = 4

# The class to use for Gunicorn worker
worker_class = sync

# Expose the configuration file in the web server
expose_config = False

# Set to true to turn on authentication
authenticate = True

# Filter the list of dags by owner name (requires authentication to be enabled)
filter_by_owner = False

[scheduler]
# Task instances to run per scheduler loop
max_tis_per_query = 512

# Should the scheduler check for pending dag runs and run them
use_row_level_locking = True

# The scheduler can run multiple threads in parallel to schedule dags
max_threads = 2

[smtp]
# If you want airflow to send emails on retries, failure, and you want to use
# the airflow.utils.email.send_email_smtp function, you have to configure an
# smtp server here
smtp_host = localhost
smtp_starttls = True
smtp_ssl = False
smtp_port = 25
smtp_mail_from = airflow@example.com

[logging]
# Logging level
logging_level = INFO

# Logging class
logging_config_class = 

# Log format
log_format = [%%(asctime)s] {%%(filename)s:%%(lineno)d} %%(levelname)s - %%(message)s
simple_log_format = %%(asctime)s %%(levelname)s - %%(message)s

[metrics]
# StatsD (Conditions: 'statsd_on' = True)
statsd_on = False
statsd_host = localhost
statsd_port = 8125
statsd_prefix = airflow

[api]
# Enable and configure API authentication
auth_backends = airflow.api.auth.backend.basic_auth,airflow.api.auth.backend.session
