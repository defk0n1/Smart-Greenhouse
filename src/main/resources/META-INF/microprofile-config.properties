# MongoDB database
jnosql.document.database=CoT_Project
jnosql.mongodb.host=127.0.0.1:27017
jnosql.document.provider=org.eclipse.jnosql.databases.mongodb.communication.MongoDBDocumentConfiguration

## Argon configuration
argon2.saltLength=32
argon2.hashLength=128
argon2.iterations=23
argon2.memory=97579
argon2.threadNumber=2

##JWT configuration
key.pair.lifetime.duration=10800
key.pair.cache.size=3
jwt.lifetime.duration=10800
jwt.issuer=urn:me.greenhouse.iam
jwt.claim.roles=groups
jwt.realm=urn:phoenix.xyz:iam
mp.jwt.token.cookie=access_token

#Email Service configuration
smtp.host=smtp.gmail.com
smtp.port=587
smtp.username=marwenbellili72@gmail.com
smtp.password=toyg luxx ljzt zjpm
smtp.starttls.enable=true

# DISABLED: Custom roles were causing incorrect mapping - role:1 was being treated as both 'Client' AND 'root'
# Only built-in roles GUEST (0) and ROOT (Long.MAX_VALUE) will be used
# roles=Client,Admin
